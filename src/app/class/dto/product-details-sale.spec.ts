import { ProductDetailsSale } from './product-details-sale';

describe('ProductDetailsSale', () => {
  it('should create an instance', () => {
    expect(new ProductDetailsSale()).toBeTruthy();
  });
});
